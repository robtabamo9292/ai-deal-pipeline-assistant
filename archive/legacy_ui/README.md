# Legacy Claude UI Prototype

This directory contains an earlier UI prototype and the script originally
used to apply it to the Streamlit application.

The current DealFlow AI application does not use these files. They are
retained only as historical implementation reference.

Do not run `apply_claude_ui.py` against the current `app.py`, because the
script may overwrite the active Streamlit interface.