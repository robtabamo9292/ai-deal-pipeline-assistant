[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
# Enforce high-signal correctness checks in CI without making formatting
# differences block product work.
select = ["E4", "E7", "E9", "F"]
ignore = ["E501"]
